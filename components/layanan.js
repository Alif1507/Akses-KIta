function layanan() {
  

  // const jktSel = data.filter( d => d.lokasi === "Jakarta Selatan" )
  // const jktPus = data.filter( d => d.lokasi === "Jakarta Pusat" )
  // const jktBar = data.filter( d => d.lokasi === "Jakarta Barat" )
  // const jktTim = data.filter( d => d.lokasi === "Jakarta Timur" )
  // const jktUt = data.filter( d => d.lokasi === "Jakarta Utara" )
  // const administrasi = data.filter( d => d.jenis === "Administrasi" )
  // const kesehatan = data.filter( d => d.jenis === "kesehatan" )
  // const asuransi = data.filter( d => d.jenis === "Asuransi" )

  // console.log(jktBar, jktPus,jktSel, jktTim,  jktUt, administrasi, asuransi, kesehatan);


  
  
  
  return $(`
   
    `);
}

export default layanan;
